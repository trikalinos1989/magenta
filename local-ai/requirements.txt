# FONI MOU local-ai — CPU-first (AMD RX 7900 XT / no CUDA)
# Install torch CPU wheels FIRST (see README), then:
#   pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
numpy>=1.26.0
soundfile>=0.12.1
librosa>=0.10.2
demucs>=4.0.1
# torch + torchaudio: install from pytorch.org CPU index (Windows AMD / no CUDA)
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
